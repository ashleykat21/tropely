workflows:
  ios-preview:
    name: Tropely iOS Preview
    max_build_duration: 60
    integrations:
      app_store_connect: Codemagic
    environment:
      groups:
        - tropely
      xcode: latest
      vars:
        BUNDLE_ID: com.nevora.tropely
        NODE: 20.17.0
    working_directory: artifacts/tropely-mobile
    scripts:
      - name: Install pnpm
        script: npm install -g pnpm@10.26.1 --force
      - name: Install dependencies
        script: cd ../.. && pnpm install --no-frozen-lockfile
      - name: Set up .env
        script: |
          echo "EXPO_PUBLIC_SUPABASE_URL=$EXPO_PUBLIC_SUPABASE_URL" > .env
          echo "EXPO_PUBLIC_SUPABASE_ANON_KEY=$EXPO_PUBLIC_SUPABASE_ANON_KEY" >> .env
          echo "EXPO_PUBLIC_MIXPANEL_TOKEN=$EXPO_PUBLIC_MIXPANEL_TOKEN" >> .env
      - name: Expo prebuild
        script: pnpm exec expo prebuild --platform ios --non-interactive
      - name: Patch Podfile for c++17
        script: |
          python3 << 'PYEOF'
          with open('ios/Podfile', 'r') as f:
              content = f.read()
          patch = """
            installer.pods_project.targets.each do |target|
              target.build_configurations.each do |config|
                config.build_settings['CLANG_CXX_LANGUAGE_STANDARD'] = 'c++17'
                config.build_settings['CLANG_CXX_LIBRARY'] = 'libc++'
              end
            end
          """
          content = content.replace('post_install do |installer|', 'post_install do |installer|\n' + patch, 1)
          with open('ios/Podfile', 'w') as f:
              f.write(content)
          print("Podfile patched successfully")
          PYEOF
      - name: Install CocoaPods
        script: cd ios && pod install
      - name: Set up keychain
        script: keychain initialize
      - name: Fetch signing files
        script: |
          app-store-connect fetch-signing-files "$BUNDLE_ID" \
            --type IOS_APP_STORE \
            --create \
            --certificate-key "$CERTIFICATE_PRIVATE_KEY"
      - name: Add certificates to keychain
        script: keychain add-certificates
      - name: Set up code signing
        script: xcode-project use-profiles
      - name: Build iOS
        script: |
          set -o pipefail
          xcodebuild archive \
            -workspace ios/Tropely.xcworkspace \
            -scheme Tropely \
            -configuration Release \
            -archivePath /tmp/Tropely.xcarchive \
            -destination "generic/platform=iOS" \
            CODE_SIGN_STYLE=Manual \
            2>&1 | tee /tmp/xcode_full.log
      - name: Show errors
        script: grep -E "error:|fatal error:" /tmp/xcode_full.log | head -30
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - /tmp/xcode_full.log
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
