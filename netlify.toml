[build]
  base = "."
  command = "cd artifacts/feltly && node_modules/.bin/vite build --config vite.config.ts"
  publish = "artifacts/feltly/dist"

[build.environment]
  NODE_VERSION = "24"
  HUSKY = "0"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
