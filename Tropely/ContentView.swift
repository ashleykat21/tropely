
import SwiftUI

// Capacitor handles all WebView rendering via its own native bridge.
// This file is intentionally minimal — the Capacitor AppDelegate in
// ios/App/App/AppDelegate.swift is the real entry point for the app.
struct ContentView: View {
    var body: some View {
        EmptyView()
    }
}
