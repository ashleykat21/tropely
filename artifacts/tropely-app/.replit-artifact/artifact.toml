kind = "mobile"
previewPath = "/mobile/"
title = "Tropely Mobile"
version = "1.0.0"
id = "artifacts/tropely-app"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
ensurePreviewReachable = "/status"
name = "expo"
paths = [ "/mobile/" ]
localPort = 24521

[services.development]
run = "pnpm --filter @workspace/tropely-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/tropely-app", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/tropely-app", "run", "serve" ]

[services.env]
PORT = "24521"
BASE_PATH = "/mobile/"
