# Clerk — get from https://dashboard.clerk.com → your app → API Keys
VITE_CLERK_PUBLISHABLE_KEY=pk_live_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: only needed if you run the dev server locally pointing at a remote API
# Leave empty in Netlify — the netlify.toml redirect handles /api/* proxying
VITE_API_BASE_URL=
