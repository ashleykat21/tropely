{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@10.26.1",
  "scripts": {
    "build": "npm install --prefix artifacts/feltly --legacy-peer-deps && npm run build --prefix artifacts/feltly",
    "typecheck:libs": "tsc --build",
    "typecheck": "tsc --build"
  },
  "private": true,
  "devDependencies": {
    "@capacitor/cli": "^8.3.1",
    "@capacitor/core": "^8.3.1",
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
